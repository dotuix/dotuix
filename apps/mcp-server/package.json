{
  "name": "@dotuix/mcp-server",
  "version": "0.1.0",
  "private": true,
  "description": "Remote HTTP MCP server for dotuix — hosted at mcp.dotuix.uts.qa",
  "type": "module",
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "start": "node dist/index.js",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@dotuix/core": "workspace:*",
    "@hono/node-server": "^1.14.2",
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "tsup": "^8.5.0",
    "typescript": "^5.8.3"
  }
}
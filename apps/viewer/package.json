{
  "name": "@dotuix/viewer",
  "version": "0.1.0",
  "description": "Desktop viewer — Tauri (Rust + WebView)",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build"
  },
  "dependencies": {
    "@dotuix/viewer-core": "workspace:*"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0",
    "typescript": "^5.8.3",
    "vite": "^6.0.0"
  }
}
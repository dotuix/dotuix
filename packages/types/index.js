/**
 * @dotuix/types runtime stub.
 * The only exported runtime value is `defineConfig` — a no-op identity
 * function that exists solely so `uix.config.ts` files can be bundled by
 * Node.js / esbuild without errors.  All real value comes from the
 * TypeScript declarations in `index.d.ts`.
 */
export function defineConfig(config) {
  return config;
}
